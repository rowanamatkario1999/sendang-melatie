export interface Tweaks {
  variant: 'classic' | 'asymmetric' | 'editorial';
  accent: string;
  titleFont: string;
  headline: string;
  eyebrow: string;
  subheadline: string;
  overlay: number;
  navLayout: 'center' | 'left';
  logoSize: number;
}
